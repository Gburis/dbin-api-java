package com.gburis.dbin.repository;

import com.gburis.dbin.model.EnderecoColeta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EnderecoColetaRepository extends JpaRepository<EnderecoColeta, Long> {
}
